{
    "name": "jaguar-expeditions/tourism-system",
    "description": "Sistema de reservas y pagos para Jaguar Expeditions",
    "type": "project",
    "require": {
        "php": ">=7.4",
        "stripe/stripe-php": "^10.0",
        "paypal/rest-api-sdk-php": "^1.14",
        "mercadopago/dx-php": "^2.5",
        "phpmailer/phpmailer": "^6.8",
        "mpdf/mpdf": "^8.2"
    },
    "require-dev": {
        "phpunit/phpunit": "^9.5"
    },
    "autoload": {
        "psr-4": {
            "JaguarExpeditions\\": "src/"
        }
    },
    "scripts": {
        "post-install-cmd": [
            "php -r \"if (!file_exists('config/config.php')) { echo 'Configuración inicial completada. Por favor revisa config/config.php'; }\""
        ]
    },
    "config": {
        "vendor-dir": "vendor",
        "optimize-autoloader": true
    },
    "minimum-stability": "stable",
    "prefer-stable": true
}
